{
  "name": "steplauncher-api",
  "version": "0.1.0",
  "private": true,
  "description": "API a medida para StepLauncher (Cloudflare Workers + Supabase).",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:build": "bun scripts/build-db-bundle.ts",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "hono": "^4.6.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.6.2",
    "wrangler": "^3.91.0"
  }
}
